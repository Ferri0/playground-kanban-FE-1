import React from 'react';
import Input from '../../ui/Input';
import Button from '../../ui/Button';

const Main = () => {
    return (
        <form>
            <Input label={'Label text'} placeholder={'placeholder text'} />
            <Button />
        </form>
    );
};

export default Main;
